name: pyright-check

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
    
jobs:
  pyright:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup
      - run: uv run pyright
